package com.vn.backend.enums;

public enum LateSubmissionPolicy {
    NOT_ALLOWED,        // không cho nộp trễ
    ALLOWED_WITH_PENALTY, // cho phép nhưng trừ điểm
    ALLOWED_NO_PENALTY  // cho phép không trừ điểm
}
