# Production Configuration
spring.application.name=backend
server.port=8081
server.servlet.context-path=/

# Database Configuration
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# JWT Configuration
spring.plugin.springsecurity.rest.token.storage.jwt.secret=KfB0zx1j7h5nxmTqksOguB7bMgy/4lFnyc6KmZrQh3MnGm02VsyiXQjZcJcmuXcD9Vibd4R6lqpYdqqFnlZ9yA==
spring.plugin.springsecurity.rest.token.storage.jwt.expiration=360000
spring.plugin.springsecurity.rest.token.storage.jwt.refresh.expiration=604800

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.pool-name=hikari-connection-pool

# Cloudinary Configuration
cloudinary.cloud-name=dgvl8woja
cloudinary.api-key=934613154799643
cloudinary.api-secret=0rrg3DpEDFVB6KgP_hft_2QyLtc

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.path=${FILE_PATH:/app/data/files}
url.download.file=${URL_DOWNLOAD_FILE:http://139.162.31.27/api/files/download/}

# Logging Configuration for Production
logging.level.root=INFO
logging.level.com.vn.backend=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate=ERROR
logging.level.org.springframework=WARN
logging.level.org.springframework.web=WARN
logging.level.org.springframework.security=WARN

# Log file configuration - sử dụng logback-spring.xml
# logging.file.name=/app/logs/backend.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS, Asia/Ho_Chi_Minh} [%thread] %-5level %X{traceId} %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS, Asia/Ho_Chi_Minh} [%thread] %-5level %X{traceId} %logger{36} - %msg%n

# Jackson Configuration
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false
spring.jackson.time-zone=Asia/Ho_Chi_Minh

# Server Configuration
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Swagger Configuration
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.packages-to-scan=com.vn.backend

# Redis Configuration
spring.data.redis.host=${REDIS_HOST:redis}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:DoAn2025PTIT}
spring.data.redis.timeout=2000ms
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:dangbinh1770@gmail.com}
spring.mail.password=${MAIL_PASSWORD:nkkm szbc ugwy mbgb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Frontend URL
frontend.url=${FRONTEND_URL:http://139.162.31.27:3000}